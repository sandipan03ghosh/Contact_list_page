{
  "name": "contactlistpage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ci-debug": "node -e \"console.log('node', process.version); console.log('platform', process.platform); const fs=require('fs'); try{ console.log('cwd', process.cwd()); console.log('files:\n' + fs.readdirSync('.').join('\n')); if(fs.existsSync('node_modules/.bin')) console.log('node_modules/.bin:\n' + fs.readdirSync('node_modules/.bin').join('\n')); } catch(e) { console.error(e) }\" && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^5.0.0"
  }
}